node_modules/
dist/
.env
.env.*
!.env.example
*.log
*.tsbuildinfo
.DS_Store

# A local clone of a personal store must never live inside this repo.
my-openstore/
*-openstore-clone/

# Personal seed data for the author's own store; never published with the project
seed/
